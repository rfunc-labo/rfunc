/**
 * Test case for invokeMiddleware.
 * Runs with mocha.
 */
'use strict'

const invokeMiddleware = require('../lib/middlewares/invoke_middleware.js')
const assert = require('assert')


describe('invoke-middleware', function () {
  this.timeout(3000)

  before(async () =>  {

  })

  after(async () =>  {

  })

  it('Invoke middleware', async () =>  {

  })
})

/* global describe, before, after, it */
