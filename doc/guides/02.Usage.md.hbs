Usage
---------

Define functions on server side.

```javascript
{{{read 'example/example-usage.js'}}}
```

rfunc client create the function dynamically on client side and you can just call it.

```javascript
{{{read 'example/example-call.js'}}}
```
